---
phase: 06-variable-compute-latency
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - stream/visualization/steady_state_trace.py
  - stream/visualization/perfetto.py
  - stream/visualization/utils.py
  - stream/visualization/constraint_optimization.py
  - stream/visualization/cost_model_evaluation_lut.py
  - stream/opt/allocation/constraint_optimization/allocation.py
  - stream/opt/allocation/constraint_optimization/utils.py
  - stream/opt/allocation/genetic_algorithm/fitness_evaluator.py
  - stream/stages/allocation/genetic_algorithm_allocation.py
  - stream/stages/set_fixed_allocation.py
  - stream/stages/set_fixed_allocation_performance.py
  - stream/cost_model/core_cost_lut.py
  - stream/stages/estimation/core_cost_estimation.py
autonomous: true
requirements: [CO-06]

must_haves:
  truths:
    - "CoreCostLUT class file is deleted from the codebase"
    - "CoreCostEstimationStage no longer creates or populates a CoreCostLUT"
    - "All 15 consumer files compile without import errors for CoreCostLUT"
    - "Visualization consumers use solved slot latency or latency_estimator instead of cost_lut"
    - "GA path consumers accept latency_estimator parameter (GA itself remains NotImplementedError)"
  artifacts:
    - path: "stream/cost_model/core_cost_lut.py"
      provides: "DELETED"
    - path: "stream/stages/estimation/core_cost_estimation.py"
      provides: "Stage that creates TileAwareLatencyEstimator only (no LUT)"
  key_links:
    - from: "stream/visualization/steady_state_trace.py"
      to: "slot_lat[slot] or latency_estimator"
      via: "uses solved slot latency for display instead of cost_lut lookup"
      pattern: "slot_lat\\[|latency_estimator"
---

<objective>
Remove CoreCostLUT and CoreCostEstimationStage's LUT logic; migrate all remaining consumer files to use TileAwareLatencyEstimator or solved values.

Purpose: Per D-04 and D-05, CoreCostLUT is removed entirely with no backward compatibility layer. All 15 consumer files are migrated. This completes the structural cleanup for Phase 6.

Output: CoreCostLUT deleted, all consumers migrated, full unit test suite passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-variable-compute-latency/06-CONTEXT.md
@.planning/phases/06-variable-compute-latency/06-RESEARCH.md
@.planning/phases/06-variable-compute-latency/06-01-SUMMARY.md
@.planning/phases/06-variable-compute-latency/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 outputs -->
From stream/cost_model/tile_aware_latency.py:
```python
class TileAwareLatencyEstimator:
    def __init__(self, workload: Workload, mapping: Mapping): ...
    def estimate(self, node: ComputationNode, core: Core,
                 inter_core_tiling: tuple[tuple[LayerDim, int], ...]) -> LatencyEstimate: ...

@dataclass
class LatencyEstimate:
    latency_total: int
    ideal_cycle: int
    energy_total: float
```

From stream/cost_model/core_cost.py (KEPT -- not deleted):
```python
@dataclass
class CoreCostEntry:
    energy_total: float
    latency_total: float
    ideal_cycle: float
    ...
    cme: CostModelEvaluation | None = None
```

Current CoreCostLUT consumers (from grep):
1. transfer_and_tensor_allocation.py -- migrated in Plan 01/02
2. steady_state_scheduler.py -- migrated in Plan 02
3. constraint_optimization_allocation.py -- migrated in Plan 02
4. core_cost_estimation.py -- partially migrated in Plan 02, LUT still created
5. steady_state_trace.py -- lines 290-296, 422-428: tta.cost_lut.get_equal_node/get_cost
6. perfetto.py -- line 94, 108: cost_lut param, passed to get_dataframe_from_scme
7. visualization/utils.py -- lines 70-134: get_spatial_utilizations, get_energy_breakdown, get_dataframe_from_scme
8. visualization/constraint_optimization.py -- line 22: cost_lut param
9. visualization/cost_model_evaluation_lut.py -- lines 58-155: visualize_cost_lut_pickle
10. allocation.py (CO) -- lines 87, 186: cost_lut in constructor and get_energies
11. utils.py (CO) -- lines 46, 101, 124: get_latencies, get_energies, get_node_latencies
12. fitness_evaluator.py (GA) -- lines 17, 21, 90: cost_lut in constructor, get_cost
13. genetic_algorithm_allocation.py -- lines 25, 50, 80, 89: REQUIRED_FIELDS, ctx.get, get_cores
14. set_fixed_allocation.py -- lines 13, 23, 43-45: REQUIRED_FIELDS, get_equal_node/core validation
15. set_fixed_allocation_performance.py -- lines 17, 27, 51-54: REQUIRED_FIELDS, get_cost for ideal_cycle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate visualization and CO/GA utility consumers</name>
  <files>
    stream/visualization/steady_state_trace.py
    stream/visualization/perfetto.py
    stream/visualization/utils.py
    stream/visualization/constraint_optimization.py
    stream/visualization/cost_model_evaluation_lut.py
    stream/opt/allocation/constraint_optimization/allocation.py
    stream/opt/allocation/constraint_optimization/utils.py
    stream/opt/allocation/genetic_algorithm/fitness_evaluator.py
    stream/stages/allocation/genetic_algorithm_allocation.py
    stream/stages/set_fixed_allocation.py
    stream/stages/set_fixed_allocation_performance.py
  </files>
  <read_first>
    stream/visualization/steady_state_trace.py
    stream/visualization/perfetto.py
    stream/visualization/utils.py
    stream/visualization/constraint_optimization.py
    stream/visualization/cost_model_evaluation_lut.py
    stream/opt/allocation/constraint_optimization/allocation.py
    stream/opt/allocation/constraint_optimization/utils.py
    stream/opt/allocation/genetic_algorithm/fitness_evaluator.py
    stream/stages/allocation/genetic_algorithm_allocation.py
    stream/stages/set_fixed_allocation.py
    stream/stages/set_fixed_allocation_performance.py
    stream/cost_model/tile_aware_latency.py
  </read_first>
  <action>
    Migrate each file to remove CoreCostLUT dependency. Per Pitfall 6 from RESEARCH.md, keep `CoreCostEntry` in `core_cost.py` -- only remove CoreCostLUT imports.

    **1. `stream/visualization/steady_state_trace.py` (lines 290-296, 422-428):**
    Per Pitfall 7 from RESEARCH.md -- the `if eq_node is not None` branch accesses `tta.cost_lut.get_equal_node(node)` to get latency for display. Replace with: always use the `slot_lat[slot]` value which is already in the else branch. Remove the `if eq_node is not None:` check and the cost_lut-based latency lookup. The `tta.cost_lut` attribute access is removed. If the function signature accepts `cost_lut`, replace with `latency_estimator: TileAwareLatencyEstimator | None = None`.

    **2. `stream/visualization/perfetto.py` (line 94, 108):**
    Replace `cost_lut: CoreCostLUT` parameter with `latency_estimator: TileAwareLatencyEstimator | None = None`. Pass through to `get_dataframe_from_scme`.

    **3. `stream/visualization/utils.py` (lines 70-134):**
    - `get_spatial_utilizations(scme, node, cost_lut)` -- the `cost_lut` branch gets `cme` from LUT. Replace: accept `latency_estimator` param, or if `cme` access is needed, keep accepting `cost_lut=None` and when None, skip spatial utilization (return defaults). Since this is visualization, graceful degradation is acceptable.
    - `get_energy_breakdown(scme, node, cost_lut)` -- same pattern. When cost_lut is None, return zeros.
    - `get_dataframe_from_scme(scme, ..., cost_lut=None)` -- already accepts `cost_lut=None` (line 120). Just ensure it works when None.

    **4. `stream/visualization/constraint_optimization.py` (line 22):**
    Replace `cost_lut: CoreCostLUT` parameter with `latency_estimator: TileAwareLatencyEstimator | None = None`. Update internal usage.

    **5. `stream/visualization/cost_model_evaluation_lut.py`:**
    This file visualizes the LUT itself. With LUT removed, this file becomes dead code. Add a deprecation comment at the top or keep it for ZigZag path compatibility. If `visualize_cost_lut_pickle` is only called from `CoreCostEstimationStage.visualize_cost_lut()` (which is already commented out on line 79), leave the file but remove the CoreCostLUT import if possible (the function accepts a generic LUT dict).

    **6. `stream/opt/allocation/constraint_optimization/allocation.py` (lines 87, 186):**
    Replace `cost_lut: CoreCostLUT` with `latency_estimator: TileAwareLatencyEstimator | None = None` in constructor. In `get_energies` call, pass `latency_estimator` instead of `cost_lut`. If energy computation needs `cost_lut`, accept graceful fallback (energy is 0 for AIE per the estimator).

    **7. `stream/opt/allocation/constraint_optimization/utils.py` (lines 46, 101, 124):**
    Replace `cost_lut` parameter in `get_latencies`, `get_energies`, `get_node_latencies` with `latency_estimator`. Use `latency_estimator.estimate(node, core, inter_core_tiling).latency_total` instead of `cost_lut.get_cost(node, core).latency_total`. For energy, return 0 (per AIECostEstimator pattern).

    **8. `stream/opt/allocation/genetic_algorithm/fitness_evaluator.py` (lines 17, 21, 90):**
    Replace `cost_lut` with `latency_estimator` in constructor. The GA path is `raise NotImplementedError` in practice per RESEARCH.md. Update `cost_lut.get_cost(equal_unique_node, core)` to `latency_estimator.estimate(...)`. Since GA is unused, this only needs to compile.

    **9. `stream/stages/allocation/genetic_algorithm_allocation.py` (lines 25, 50, 80, 89):**
    Replace `"cost_lut"` in REQUIRED_FIELDS. Replace `self.cost_lut` with `self.latency_estimator = self.ctx.get("latency_estimator")`. Update the fitness evaluator construction.

    **10. `stream/stages/set_fixed_allocation.py` (lines 13, 23, 43-45):**
    Per Pitfall 5 -- the `cost_lut.get_equal_node(node)` and `get_equal_core(...)` are sanity checks. Replace with: assert node exists in mapping directly (`assert self.mapping.get(node) is not None`). Remove `"cost_lut"` from REQUIRED_FIELDS.

    **11. `stream/stages/set_fixed_allocation_performance.py` (lines 17, 27, 51-54):**
    Per Pitfall 5 -- calls `cost_lut.get_cost(equal_node, core)` to get `ideal_cycle` for `get_cycles_scaled(utilization)`. Replace with: use `latency_estimator.estimate(node, core, inter_core_tiling).ideal_cycle`. Get utilization from `mapping.get(node).kernel.utilization`. Compute `cycles_scaled = ideal_cycle * 100.0 / utilization` (replicating `CoreCostEntry.get_cycles_scaled`).

    For each file: remove `from stream.cost_model.core_cost_lut import CoreCostLUT` import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -x -v --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from stream.cost_model.core_cost_lut import" stream/` returns ONLY `stream/cost_model/core_cost_lut.py` itself (if any) and no consumer files
    - `grep -r "CoreCostLUT" stream/visualization/` returns no matches
    - `grep -r "CoreCostLUT" stream/stages/set_fixed_allocation` returns no matches
    - `grep -r "CoreCostLUT" stream/opt/allocation/genetic_algorithm/` returns no matches
    - All unit tests pass
  </acceptance_criteria>
  <done>All 11 consumer files migrated away from CoreCostLUT. No file outside core_cost_lut.py itself imports CoreCostLUT.</done>
</task>

<task type="auto">
  <name>Task 2: Delete CoreCostLUT and strip LUT logic from CoreCostEstimationStage</name>
  <files>
    stream/cost_model/core_cost_lut.py
    stream/stages/estimation/core_cost_estimation.py
  </files>
  <read_first>
    stream/cost_model/core_cost_lut.py
    stream/stages/estimation/core_cost_estimation.py
    stream/cost_model/tile_aware_latency.py
  </read_first>
  <action>
    **A. Delete `stream/cost_model/core_cost_lut.py`:**
    Remove the file entirely per D-04. `CoreCostEntry` in `core_cost.py` is kept (per Pitfall 6 -- ZigZag estimator returns it).

    **B. Rewrite `CoreCostEstimationStage` in `stream/stages/estimation/core_cost_estimation.py`:**
    The stage currently:
    1. Creates a CoreCostLUT and populates it by running estimators
    2. Sets `cost_lut` on ctx

    Replace with:
    1. Keep `valid_allocations` logic (nodes -> cores mapping) -- this is useful for assertions
    2. Create `TileAwareLatencyEstimator(self.workload, self.mapping)` instead of CoreCostLUT
    3. Set `latency_estimator` on ctx: `self.ctx.set(workload=self.workload, accelerator=self.accelerator, latency_estimator=self.latency_estimator)`
    4. Remove `cost_lut` from ctx.set entirely
    5. Remove `update_cost_lut()` method
    6. Remove `cost_lut_path` and pickle logic
    7. Remove `visualize_cost_lut()` method (already commented out)
    8. Keep `get_estimator()` and `is_aie_compute_core()` if needed by visualization, or remove if no longer referenced
    9. Remove `from stream.cost_model.core_cost_lut import CoreCostLUT` import

    The stage class name `CoreCostEstimationStage` can stay (renaming is a larger refactor that would touch pipeline configs). Just update its internals.

    **C. Verify no remaining imports:**
    Run `grep -r "core_cost_lut" stream/` to confirm zero references remain.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -x -v --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f stream/cost_model/core_cost_lut.py && echo "DELETED"` prints DELETED
    - `grep -r "core_cost_lut" stream/` returns no matches
    - `grep -r "CoreCostLUT" stream/` returns no matches
    - `grep "TileAwareLatencyEstimator" stream/stages/estimation/core_cost_estimation.py` returns a match
    - `grep "cost_lut" stream/stages/estimation/core_cost_estimation.py` returns no matches
    - All unit tests pass
  </acceptance_criteria>
  <done>CoreCostLUT is deleted. CoreCostEstimationStage creates TileAwareLatencyEstimator only. Zero references to CoreCostLUT remain in the codebase.</done>
</task>

</tasks>

<verification>
1. `.venv/bin/pytest tests/unit/ -x` -- all unit tests pass
2. `grep -r "CoreCostLUT" stream/` -- zero matches
3. `grep -r "core_cost_lut" stream/` -- zero matches
4. `test ! -f stream/cost_model/core_cost_lut.py` -- file does not exist
5. `python -c "from stream.cost_model.tile_aware_latency import TileAwareLatencyEstimator; print('OK')"` -- imports cleanly
</verification>

<success_criteria>
- CoreCostLUT class file is deleted per D-04
- All 15 consumer files compile without CoreCostLUT import errors per D-05
- CoreCostEstimationStage creates TileAwareLatencyEstimator, not CoreCostLUT
- CoreCostEntry is preserved in core_cost.py for ZigZag compatibility
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-variable-compute-latency/06-03-SUMMARY.md`
</output>
